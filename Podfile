# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'DailyTracker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DailyTracker
  pod 'XCGLogger', '~> 4.0.0'

  target 'DailyTrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DailyTrackerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'Watch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Watch

end

target 'Watch Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Watch Extension

end
